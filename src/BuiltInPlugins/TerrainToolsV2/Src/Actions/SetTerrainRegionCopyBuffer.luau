return require(script.Parent.Action)(script.Name, function(v0, v1, v2)
    return {
        TerrainRegionCopyBuffer = v0, 
        TerrainRegionCopyTransform = v1, 
        TerrainRegionCopySize = v2
    };
end);
