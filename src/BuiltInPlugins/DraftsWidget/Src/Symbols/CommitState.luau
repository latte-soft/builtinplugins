local v0 = require(script.Parent.Parent.Parent.Src.Util.Symbol);
return {
    Uncommitted = v0.named("CommitStatusUncommitted"), 
    Committing = v0.named("CommitStatusCommitting"), 
    Committed = v0.named("CommitStatusCommitted")
};
