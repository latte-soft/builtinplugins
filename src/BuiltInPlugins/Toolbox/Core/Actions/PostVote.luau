return require(script.Parent.Parent.Parent.Packages.Framework).Util.Action(script.Name, function(v0, v1, v2)
    return {
        assetId = v0, 
        voteResponse = v1, 
        userVote = v2
    };
end);
